# Crosshair Overlay

A lightweight, always-on-top crosshair overlay for Windows that inverts the colors behind it — Minecraft style.

## Usage

Run the app or grab the EXE from [Releases](../../releases).

```
python app.py
```
